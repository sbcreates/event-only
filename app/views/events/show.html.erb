<%= render 'event', event: @event %>

<%= link_to "Edit Event", edit_event_path(@event) %> | <%= link_to 'Delete Event', events_path(@event), method: :delete, date: {confirm: "Are you sure you want to delete '#{@event.name}'?"} %>
